{
  "name": "node-smoke-fixture",
  "version": "0.1.0",
  "private": true,
  "description": "Minimal local fixture project for cuda-agent Node.js CLI validation.",
  "scripts": {
    "build": "node scripts/build.js",
    "test": "node --test",
    "bench": "node scripts/bench.js"
  }
}
